# 交接文档 · 糊涂蛋工作台（永久版）

> 本文档给「接手本项目的下一位助手」看。把源头、决策、结构、未完事项一次写清，新助手读完即可无缝接手，不必重问老板。

---

## 〇、给新助手的开场单（换助手必读，照着做）

你好，新助手。我是「打边炉」——这个工作台项目的总负责人（老板定的名字和角色）。
请先记住下面这几条，然后直接去翻「一、项目源头」和「三、当前进度」：

1. **老板是谁**：本项目的老板（用户）是这个产品的 owner，你对他只用大白话，技术方案必须紧跟人话解释。
2. **你的身份**：你是「打边炉」，项目总负责人，全栈（需求/设计/架构/写码/测试/部署/验收都你干），老板只拍板和点头。
3. **最高纪律 = 10 条红线**（详见文档末尾附录）：先出主意再动手、只做点名的事、说话全用大白话（不许蹦英文词和专业术语）、老板说错可反驳但先查实、大方向含糊先问清、活全你干、先查实情再答、眼光放长远、改界面样子先经老板同意、每次改完附在手机/电脑真打开看的清单。
3.5. **【最强铁律·压过一切】每次回复前，先在心里把红线逐条过一遍、查自己有没有踩线**：发出任何一段话之前，先默念自查——有没有擅自改东西、有没有蹦英文词/专业术语、有没有替老板下结论、有没有漏验收清单。这条和"说人话"并列最高，漏查就是失职。新助手第一句话起就执行，换模型/换会话也不断。
4. **项目在哪**：所有文件在 `D:\EDG\software\Workbench`（Windows 路径）。这是一个**本地网页应用**，不是微信小程序，也不是那两个会到期的小程序项目。代码已部署到 GitHub Pages（线上地址见「四」）。
5. **现在做到哪了**：功能代码已写完并上线（当前版本 **V1.0.62**，即 `index.html` 单文件内联版）；Supabase 后端已建好（老板邮箱 `fashpig51@163.com` 注册的项目 `ktdlawiitofkhgklhyrz`）、密钥已填、数据已实测跨设备同步成功；手机端响应式 + 导出导入加密备份 + 自动同步均已完成；**第四层私有仓库每日自动加密备份已上线（北京时间 03:00）**。⚠️ **架构事实（必读）**：上面 V1.0.33~V1.0.38 写的是 `assets/js` 多文件版，那套**从未部署**、老板从未看到；线上实际运行的是 **`index.html` 单文件内联版**（约2582行，自带同步/主题，不加载 `app.js` 等）。本版（V1.0.42）改动：**待办页默认只显"未完成"、原"已完成"按钮改为「📜 历史」入口**；主题默认浅色（右上角可切深）；**V1.0.40 同步引擎升级：云端存储由"整批删掉重传"改为"按编号增量更新"，消除删完插失败的数据空窗，本地删除现在会逐条同步删到云端（不再整批清）**。**V1.0.41 同步加固**：①合并防覆盖——本地有未上传改动的记录用本地 `_dirty` 标记保本地、不被云端旧版盖掉，已删待推的也不再复活；②待办标题与笔记内容上云前加密（云端只存密文，口令不对读不出）；③自动同步轮询由 8 秒缩到 3 秒；④未记住登录/无缓存时登录框秒出、云端后台静默拉取不挡首屏。**V1.0.57 备忘录全新重做为结构化清单**。**V1.0.58 修复手机端清单项文字不显示**。**V1.0.61 紧急回滚 V1.0.60 并修复误伤**。**V1.0.62 修复待办密文显示并改回两栏纯文本备忘录**。详见「三」。
6. **接手第一件事**：**先读 `index.html`（单文件内联版，约 2582 行，线上真正在跑的那个）**，这是你以后改功能的主战场；再读 `README_setup.md` 了解部署、读 `supabase/schema.sql` 了解多文件设计版的数据结构（注意：线上单文件版实际只用 5 张 `wb2_*` 表，见下方架构事实）。**`assets/js/` 下的 app.js/sections.js/store.js 是另一套平行但未被部署的实现（死代码），不要去读它来理解线上逻辑**。别急着改代码，先理解再提方案。
7. **口令机制**（核心）：数据靠老板自设的「口令」加密 + 分区。同一口令多设备共享同份数据；口令忘了数据解不开。新助手不要试图"重置"或"找回"口令，那是老板的私物。
8. **有事先问老板、别擅自改**：尤其涉及 UI 长相、换技术方案、动数据结构，必须先出方案等点头。
9. **当前版本号：V1.0.62**（功能版本号，随实质改动递增）。版本号规则：`1.⌊提交数÷100⌋.提交数%100`，随提交数递增——100 个提交→V1.1.0；156 个→V1.1.56；238 个→V1.2.38（末尾两位=提交数后两位，满 100 时中间位+1、末尾不归零）。版本真源：**`index.html` 里的 `APP_VERSION` 常量**（单一真源，备份/交付时以此为准）。⚠️ **重大架构事实（必读）**：线上 GitHub Pages 实际部署并运行的是 **`index.html` 单文件内联版**（约2582行，自带同步/主题逻辑，**不加载** `assets/js/app.js`/`sections.js`/`store.js`/`config.js`）；之前维护的多文件版（V1.0.33~V1.0.38 那些改动）**从未被部署、老板也从未看到**，是一套平行但未被使用的实现。**以后要改功能，必须改 `index.html` 这套**，不要去动那套多文件版（除非先切换部署）。每次有实质改动提交后，须同步 `APP_VERSION` 的值。

---

## 一、项目源头（从哪来、为什么这样定）

### 1.1 缘起
老板想要一个**永久的**工作台，覆盖「工作 / 学习 / 日常生活」三块，电脑、手机、iPad 都要用，且**一台设备改了另一台立刻同步**（例如手机点完成待办，另一台手机也同步打勾）。由老板任命「打边炉」为总负责人，定下 10 条红线作为最高纪律。

### 1.2 关键决策过程（都是老板拍板的，不是助手自作主张）
- **形态**：本地网页（浏览器打开即用，可"加到主屏幕"当 App）。理由：不被任何服务商卡脖子，最"永久"。
- **数据存放**：每台设备本地留缓存（断网也能看）+ 云端同步混合。主数据在云端（老板的 Supabase 免费项目 `ktdlawiitofkhgklhyrz`）。
- **同步中转**：用 **Supabase 免费档**当"传话站"，实现手机↔手机实时同步。
- **为什么不用那两个小程序服务器做同步**：老板的两个微信小程序（糊涂蛋多吃饭 / 糊涂蛋的小仓库）服务器都是**体验版、会到期**。把永久工作台架在会断的梯子上不合理——所以本工作台**不依赖任何会到期的服务**。
- **为什么不用消费级网盘（百度/OneDrive）做同步**：消费级网盘的自动同步是"电脑系统级"功能，网页应用（尤其手机浏览器）打不开电脑里的网盘文件夹，满足不了"手机↔手机实时同步"。因此必须有一个"手机能直接对话的传话站"= 后端。
- **为什么选 Supabase 而不是 Cloudflare**：老板倾向 B 方案（Supabase）。Supabase 免费档自带"数据一变就推给另一端"的实时功能，同步最省事。核实过：免费档 200 并发 / 月 200 万条推送，个人用几辈子用不完；7 天无活动会**自动暂停但数据不丢**，已用"保活定时任务"破解。
- **Cloudflare 已弃用**：早期建过 Cloudflare Pages 项目，但实测 `pages.dev` 域名在中国大陆被墙/超时（HTTP 000），无法用作国内可访问的部署。老板已在 Cloudflare 后台建过项目，**建议直接删掉**（免费档、删了不影响现有功能：数据在 Supabase、网页在 GitHub Pages、备份在私有仓库，三者与 Cloudflare 零耦合）。
- **致命坑（已解决）**：Supabase 的"匿名登录"会把每台设备当不同用户、数据隔离，正好违背"多设备共享同份数据"。破解法 = **不依赖它的账号体系**，改用"所有设备用同一口令"的设计（见 1.3）。
- **数据表 = 分表**：老板拍板用分表（todos / notes / books / habits 各一张），结构清晰、好维护、加功能不打架。

### 1.3 口令加密与多设备共享机制（核心设计，务必理解）
- 老板自设一个口令（不注册账号）。
- 前端用 Web Crypto（AES-GCM）以口令派生密钥，把敏感字段（标题/正文/分类等）**加密后再存云端** → 云端只存密文，外人拿到也打不开。
- `workspace_id = SHA256(口令)` 前 16 位，作为数据分区键。同一口令 → 同一分区 → 同份数据；不同口令 → 不同分区、互相看不到。
- 因此"多设备同步"靠：各设备输入同一口令 → 落到同一 `workspace_id` → 读写同一批行 → 实时推送互通。
- **风险点**：口令忘了 = 数据解不开（云端是密文）。这是设计权衡，已在 UI 弹窗和文档里反复提醒老板牢记口令。

### 1.4 界面方向（已老板批准，红线⑨）
**V1.0.36 起界面定稿为**：深黑灰底（主背景 `#0b0f17`）+ 墨绿/青绿强调色（薄荷绿 `#34e0c4`）+ 大留白 + 卡片式 + **顶部4导航（总览/工作/学习/生活，电脑端左侧栏、手机端底部栏）**。浅蓝白那套是过渡稿，已弃用（即 `body.light`，V1.0.37 起默认开、打开即为浅色；右上角月亮可切深）。
**布局定稿**（V1.0.36 老板认可）：顶栏（左品牌名 / 中胶囊搜索框 / 右 日期+铃铛+主题切换+导出+导入+锁+账户头像）；总览页 = 4 张数据卡（今日待办/进行中/已完成/专注）+ 两栏（左今日任务清单、右时间规划时间轴+近期到期提醒）。第一版效果图在 `workbench_mockup.html`（仅供参考，与现版不同）。
**专属图标（已老板选定为"清单版"）**：深色圆角底 + 浅色玻璃卡片 + 三行清单（首行薄荷绿勾、其余空心框）+ 右侧横线；桌面名字统一为"糊涂蛋工作台"。图标文件在 `assets/icons/`（192/512/苹果180/maskable 四套）。

---

## 二、当前文件结构（D:\EDG\software\Workbench）

```
Workbench/
├── index.html                # 网页外壳（侧栏+顶栏+内容区+口令弹窗；已引图标/apple-touch）
├── manifest.json             # 加到主屏幕用的配置（含专属图标引用 assets/icons/）
├── assets/
│   ├── css/style.css         # 深色玻璃风格样式
│   ├── icons/                # 专属图标（清单版：深色底+薄荷绿勾，名字"糊涂蛋工作台"）
│   │   ├── icon-192.png
│   │   ├── icon-512.png
│   │   ├── icon-maskable-512.png
│   │   └── apple-touch-icon.png
│   └── js/
│       ├── config.js         # Supabase 两个密钥已填好（项目 ktdlawiitofkhgklhyrz）
│       ├── crypto.js         # 口令加密（AES-GCM）+ workspace_id 派生
│       ├── store.js          # 本地缓存 + Supabase 云端读写 + 实时订阅 + 心跳
│       ├── ui.js             # 界面小工具（建节点/转义/提示条/同步状态灯）
│       ├── sections.js       # 三块功能：工作/学习/日常生活
│       ├── app.js            # 主控制器：口令登录、导航、顶栏、同步状态
│       └── lib/supabase-js.global.js  # 本地化的 Supabase 库（已去掉外部 jsDelivr 依赖）
├── supabase/
│   └── schema.sql            # 8 张分表（todos/notes/books/habits/goals/time_logs/moods/health）——这是**多文件设计版**的 schema；⚠️ 线上单文件 `index.html` 实际只用 **5 张 `wb2_*` 表**（wb2_todos / wb2_notes / wb2_pomodoros / wb2_folders_v1 / wb2_pomo_projects），改表结构前务必先确认动的是哪套。
├── scripts/keepalive/
│   ├── keepalive.yml         # GitHub Actions 保活任务（每3天戳库防暂停，已生效）
│   └── README.md             # 保活部署说明
├── README_setup.md           # 老板自部署指南（现已部署好，主要作历史/接手参考）
├── workbench_mockup.html     # 第一版 UI 效果图（参考）
└── 交接文档_糊涂蛋工作台.md  # 本文件
```

> 注：`assets/js/lib/supabase-js.global.js` 是**本地副本**，原本从 `cdn.jsdelivr.net` 加载（国内不稳），已改为引用本地，减少一个出问题的隐患。

---

## 三、当前进度（做到哪了 / 还差什么）

> ⚠️ **读本节前先看开场单第 5、9 条（架构事实）**：下方「已完成」清单是按**多文件设计版（V1.0.33~V1.0.38 那套）**记录的功能全貌，但**线上真正运行的是 `index.html` 单文件内联版**（默认浅色、功能以待办/笔记/番茄钟/文件夹为主，同步引擎见 V1.0.40/41）。两版功能范围不完全一致——**线上单文件版当前实际交付的功能，以直接打开 `index.html` 为准**，不要凭下方清单假设某功能一定在线上。改功能必须改 `index.html`。

### 已完成（代码已写、逻辑自洽、且已真机验证）
- [x] 网页外壳：深色玻璃 UI、左侧三栏导航、顶栏问候+日期+同步状态灯、搜索、锁屏。
- [x] 口令登录：输入口令 → 派生密钥与分区 → 进入。
- [x] 加密：AES-GCM，敏感字段云端存密文。
- [x] 本地缓存：localStorage，断网可看已缓存内容。
- [x] 工作板块：待办增删改、完成勾选、优先级、截止日、筛选；日历视图（按月翻页、每天小圆点、点日期看当天待办）。
- [x] 学习板块：笔记（标题/内容/标签/分类/模板/附件/双向链接/块编辑/渐进式总结/语音速记/每日笔记）、书单（想看/在看/看完/进度%/评分/感想）、关系图谱、白板画布。
- [x] 日常生活板块：习惯打卡（量化数值/连续天数/补卡/周网格/提醒时间）、心情日记、健康记录（睡眠/喝水/运动/体重/用药/步数）、统计报告。
- [x] 全局功能：首页总览仪表盘、目标管理（添加/关键结果+1/完成/删除）、每周自动复盘、时间日志、日历融合、甘特图/时间轴、暗/亮主题切换、自然语言输入、导出本周报表/全部加密备份。
- [x] 云端同步逻辑：store.js 已写好 upsert / remove / 实时 subscribe，配置好密钥即生效。
- [x] 数据库结构脚本 `schema.sql` 写好（4 表 + RLS + 实时），**已在 Supabase 执行**。
- [x] **Supabase 真实项目已建**：老板邮箱 `fashpig51@163.com` 注册，项目 `ktdlawiitofkhgklhyrz`，密钥已填进 `config.js`。
- [x] **已部署到 GitHub Pages**：仓库 `fashpig51/hutudan-workbench`，线上地址 **https://fashpig51.github.io/hutudan-workbench/**（首次构建约几分钟）。
- [x] **保活任务已配并验证**：`.github/workflows/keepalive.yml` 每 3 天戳库，已实测 success。
- [x] **三端真机验收通过**（老板验完）：跨设备同口令看同份数据、手机A加一条手机B 5 秒内出现、断网仍可见缓存、无红色报错。
- [x] **专属图标已配置**：清单版（深色玻璃+薄荷绿勾），名字"糊涂蛋工作台"，manifest + apple-touch 已接好。
- [x] **手机端响应式布局（方案B）**：窄屏（≤720px）自动把左侧导航改成底部一排图标（类微信底栏），主区独占整屏，解决"手机看不全内容、文字竖排"的问题。实现在 `style.css` 的 `@media (max-width:720px)` 块。
- [x] **导出/导入加密备份（方案C）**：顶栏"📤备份 / 📥恢复"按钮。导出 = 把当前所用表（线上单文件版为 **5 张 `wb2_*` 表**）整库明文打包 → 用老板口令 AES-GCM 加密 → 下载成 `.json` 文件（落到别人手里也打不开）；恢复 = 选该文件 → 同口令解密 → 逐条写回各表 → 刷新。配合"**记牢口令**"构成本地数据兜底。逻辑在单文件版 `index.html` 内的导出/导入函数（非 `app.js`，`app.js` 是死代码）。
- [x] **iPhone 顶栏刘海遮挡修复**：`style.css` 手机端 `@media(max-width:720px)` 的 `#topbar` 上内边距改为 `calc(14px + env(safe-area-inset-top,0px))`，苹果自动空出刘海高度，安卓不变。老板 iPhone 验收通过。
- [x] **第四层：私有仓库每日自动加密备份（老板批准，不放公开仓库）**：独立私有仓库 `fashpig51/hutudan-workbench-backup`（private:true，外人进不去）。每天**北京时间 03:00**（cron `0 19 * * *` UTC）GitHub Actions 自动把云端该 workspace 的 4 张表加密行拉一份，存进 `backups/YYYY-MM-DD.json`。数据仍是密文、口令不进仓库（备份靠 workspace 指纹 `c5dfd3eb88fa5ce4` 抓数据）。本地仓库路径 `D:\EDG\software\hutudan-workbench-backup`，含 `backup.yml` 定时任务、`backup.js` 拉数据脚本、`restore.js` 还原脚本、README。已真实验证 run#5 success、生成 `2026-08-22.json`、内容为密文无明文。
- [x] **导入兼容两种备份格式（已修复）**：前端"导出"生成 `{enc:密文}`（整包加密）；自动备份仓库生成 `{tables:{...}}`（字段已是密文，外层无 enc）。`app.js` 的 `importData` 现兼容两种；`store.js` 新增 `upsertRaw` 方法（字段已是密文时不再二次加密，否则会变成"密文的密文"解不开）。文件选择器接受 `.json` 和 `.txt`。
- [x] **V1.0.36 页面重设计（已上线 GitHub Pages）**：老板觉得旧版页面"拉跨、手机端折叠遮挡"，要求重做。这轮**只改皮肤与布局、不动数据结构和同步逻辑**。改动点：①整套配色换成深黑灰底（`#0b0f17`）+ 墨绿强调（`#34e0c4`）；浅蓝白稿弃用（留作 `body.light` 备选）。②顶栏改为：左品牌名 / 中胶囊搜索框 / 右 日期+铃铛+主题切换+导出+导入+锁+账户头像。③导航改为顶部4项（总览/工作/学习/生活）：电脑端左侧栏、手机端底部栏（`env(safe-area-inset-bottom)` 防 Home 条遮挡）。④总览页重构：4 张数据卡（今日待办/进行中/已完成/专注）+ 两栏（左今日任务清单含逾期标红/临近标蓝、右时间规划时间轴+近期到期提醒）。⑤**新增日历日/周视图**（工作板块，原只有月视图）：月/周/日切换，周视图以周一起始。⑥**新增标签全局增删改**：工作和学习都加"⚙ 管理标签"弹窗（新建/改名/删除，删除会从所有记录移除该标签），填标签时输入框自动提示已有标签。改的文件：`assets/css/style.css`（整体换肤+布局）、`assets/js/app.js`（顶栏+总览）、`assets/js/sections.js`（日历日周视图+标签管理）。已 `node --check` 语法通过、本地起服务资源全 200、周起始逻辑验证正确（8-23 周日→算到 8-17 周一）。已推 GitHub（`f0d6a75` + 版本号对齐 `0488868`），线上 `https://fashpig51.github.io/hutudan-workbench/` 已更新为 V1.0.36。

### 待完成 / 后续可优化（非必须，需老板点头才做）
- [ ] 全功能已上线，进入细节打磨期。老板集中发小 bug 后逐项修复。
- [ ] 后续可优化项（均需先出方案）：更精细的数据可视化图表、浏览器原生通知提醒、AI 语音识别付费升级、图标美术稿精修。
- [ ] 若老板未来要"完全离线也要多设备同步"，再评估 P2P/本地网盘方案（当前架构不支持，属重大改动，须单独立项）。

### 已知坑 & 老板实测踩过的雷（新助手必读，别让老板再踩）
1. **从 GitHub 下载备份文件的正确姿势**：不能直接点文件页面下载（会下到 GitHub 包了 HTML 的网页版，导入时报"不是备份文件"）；必须进文件页点 **Raw** 按钮 → 在纯文本页面右键/长按"保存" → 文件名手动改成 `xxx.json`（浏览器可能默认存成 .txt 或无扩展名，系统还可能乱识别成 Adobe 等，都不影响，只要内容是 JSON 就能恢复）。详见私有备份仓库 README。
2. **导入报"这不是工作台备份文件"的两类原因**：①下成了 HTML 网页版（按上条用 Raw 重新下）；②用的是自动备份仓库的文件但代码是旧版（现已兼容，刷新页面即可）。
3. **Cloudflare 已弃用**：早期建过 Cloudflare Pages 项目，但 `pages.dev` 域名在中国大陆被墙/超时（实测 HTTP 000），已弃用，数据不走它。老板已在 Cloudflare 后台建过项目，建议删掉（免费档、删了不影响现有功能）。现部署链路只三样：Supabase（数据）+ GitHub Pages（网页）+ 私有仓库（每日 03:00 备份）。
4. **本地改代码推送注意（2026-08-23 实测更新）**：这台电脑（`D:\EDG\software\Workbench`）**没有配 GitHub 账号/SSH key**，平时 `git push` 会因要登录而卡住、且网络时通时不通（当天曾出现 "Connection was reset" 连不上，重试后又能推）。**可用老板给的 Personal Access Token 临时推送**：把远端 URL 临时改成 `https://<TOKEN>@github.com/fashpig51/hutudan-workbench.git` 再 `git push`，推完**立刻把远端 URL 改回 `https://github.com/...`** 抹掉令牌（令牌千万别留进配置或提交）。若仍连不通，再改用 GitHub 网页直接传文件或文件接口（API）更新。

### 已验证 / 未验证
- 已做：所有 JS 文件语法校验通过；逻辑自洽性已通读；**端到端跨设备实时同步真机实测通过**（老板验收）。
- 外网依赖：已把 Supabase 库本地化（去 jsDelivr），页面仅依赖 GitHub Pages 自身，减少国内抽风风险。

---

## 四、怎么跑起来（给新助手）

**线上地址（已部署，直接用）：**
**https://fashpig51.github.io/hutudan-workbench/**

**加到手机桌面（当 App 用）：**
- 苹果：用 **Safari** 打开上面地址 → 底部分享键 → 「添加到主屏幕」→ 名字「糊涂蛋工作台」。
- 安卓：用 **Chrome** 打开上面地址 → 右上角三个点 → 「添加到主屏幕」/「安装应用」。
- 注意：**别在微信里打开去加**，微信内置浏览器加不了桌面。

**纯本地预览（不联网也能看，但无同步）：**
```
cd D:\EDG\software\Workbench
python -m http.server 8080
# 浏览器开 http://localhost:8080
```
首次打开会让你设口令，随便输一个（纯本地模式数据只存在本机浏览器）。

**开启/验证同步：** 多设备输同一口令即同步（Supabase 已配好）。

**改完代码怎么上线：** 在 `D:\EDG\software\Workbench` 里 `git add -A && git commit && git push`，GitHub Pages 会自动重建（几分钟生效）。保活任务需要的 `SUPABASE_URL` / `SUPABASE_ANON` 两个密钥已作为仓库 Secrets 配好。

---

## 五、备份与版本

- 应用版本号：**`index.html` 里的 `APP_VERSION` 常量**（当前 `V1.0.62`，功能版本号）。⚠️ 线上跑的是 `index.html` 单文件内联版，不是 `assets/js/config.js` 那套；版本号规则见开场单第 9 条；每次有实质改动提交后须同步此值。
- **V1.0.56（2026-08-27）**：备忘录看页面子说明缩进不一致修复——原逻辑会保留子说明行首用户输入的空格/制表符（如“  ❌找不到”比“✔️拼多多”多缩进），且星号后无空格（如“★5.”）不被识别为列表项、其下子说明不缩进；现统一清掉子说明行首空白，并把星号/横线后的空格改为可选，所有子说明缩进统一由 CSS 控制。
- **V1.0.57（2026-08-27）**：备忘录**全新重做（不保留旧 UI、功能也重做）**——老板因旧「文本+预览」渲染反复修不好、点名彻底重做。改为「结构化清单」：笔记内容从纯文本改为存 JSON 清单（`{v:2,type:"checklist",items:[...]}`），每条清单项可勾选完成、标星★、切换状态（点一下出 ❌、再点出 ✔️、再点清空；老板用来标"找不到/已采购"）、缩进成子项、上下移、删除；★❌✔️ 从手打符号变成可点按钮。旧纯文本笔记打开时自动转成清单（尽量保留原有勾选/星标/状态）。新建/编辑实时保存并同步云端；导出按清单导出 Markdown。配套把路由 `go()`、置顶/加密/删除/新建/搜索/文件夹 等交互全部接到新清单模型上；旧 `renderMarkdown`/`.notes-*` 等代码与样式保留不删（无害死代码）。
- **V1.0.58（2026-08-27）**：修复手机端备忘录清单项文字不显示——清单项输入框在部分移动浏览器里被压到宽度为 0，导致只看见按钮、看不见文字；现给输入框加最小宽度 80px、统一外观、placeholder 颜色，确保文字可见。同时加数据保护：旧纯文本转换时若去掉前缀后文字为空，则保留原始行文字；若已损坏的 checklist JSON 里全部文字为空，自动从原内容恢复并标"文字丢失"提示。
- **V1.0.59（2026-08-27）**：备忘录**按苹果备忘录风格二次重做（老板嫌 V1.0.57 太花、按钮太多）**——每行只保留一个小圆圈，点一下在「没勾 → 打勾(完成/已采购) → 标找不到(❌) → 没勾」三态间循环；删除键平时隐藏、仅当你点进那行编辑时才出现；置顶/标星/加密/导出/删除全部收进右上角「⋯」菜单，不再摊在面上。旧笔记打开时自动把「书名 + 缩进的 ❌/✔️ 备注」合并成一行（如「大学生… · 找不到，采购 ¥32.3」），去掉了之前每行带的上下箭头、独立星标键、独立状态键等。标星改为整篇笔记标星（同苹果备忘录）。
- **V1.0.62（2026-08-27）**：**修复待办标题显示成 `__ENC__:` 密文，并把备忘录改回两栏纯文本记事本**——根因：`tryUnlock` 把云端加密待办直接写进内存/本地缓存，没有解密标题；且 `loadLocal()` 后无兜底解密。修复：①`tryUnlock` 里对 todos 标题逐条解密后再赋值；②新增 `decryptState()` 在 `loadLocal()` 之后兜底遍历解密待办标题和笔记内容；③保持 `syncLoad` 原有解密逻辑。备忘录方面，老板反馈 V1.0.61 三栏清单版"看的位置太小、点不进去"，改回 V1.0.60 的两栏纯文本记事本：左侧笔记列表固定 320px、右侧大文本框占满剩余空间，没有任何勾选框/圆圈/状态按钮，点开直接看、直接改；旧清单 JSON 笔记打开自动还原为纯文字；新建/删除/搜索保持可用。同时注意保留 `renderFolders` 等必要函数不再被 `render()` 调用，避免再触发待办渲染中断。
- **V1.0.61（2026-08-27）**：**紧急回滚 V1.0.60 并修复误伤**——老板验收 V1.0.60 后指出：①备忘录"看的位置太小"、要改回 V1.0.58 三栏清单版；②待办事项页面渲染异常（V1.0.60 删掉 `renderFolders` 但 `render()` 仍调用它，导致 JS 异常中断、待办页等后续视图渲染失败）。本版从 GitHub 历史版本直接恢复 V1.0.58 的 `index.html`，版本号升为 V1.0.61；恢复三栏清单布局（文件夹/列表/编辑）、每行小圆圈+星标+状态+上下移+删除按钮、旧笔记自动转清单。`renderFolders` 等函数恢复，`render()` 不再报错，待办页恢复正常。
- **V1.0.60（2026-08-27）**：备忘录**彻底重做为极简纯文本记事本（老板第三次点名推翻重做，明确"不要勾选框、不要花里胡哨、只方便查看和编辑、不保留现在的功能"）**——删除全部勾选框/圆圈/状态按钮/清单/文件夹功能，改为「左侧笔记列表 + 右侧大文本框」，点开即改、输入实时保存并同步云端；旧清单 JSON 笔记打开自动还原为纯文字、老文本笔记原样显示，数据不丢。配套路由 `go()`、新建/删除/搜索 全部接到纯文本模型；`renderFolders`/`renderMarkdown` 等旧函数已移除，UI 极简（仅"新建笔记"按钮 + 编辑器内一个删除图标）。**此版已废弃，V1.0.61 已回滚**。
- **V1.0.55（2026-08-27）**：备忘录看页面有序项识别放宽——原只认「行首直接 1. 」（半角点+点后空格），老板笔记里数字项前有空格/Tab、或用全角点「1．」/顿号「1、」时整篇塌成无圆点无缩进；现改为行首允许空白、半角/全角点/顿号都认，Node 实测 7 种写法全部正确出圆点+缩进。
- **备份规矩（老板红线）**：任何备份/打包本项目源码或数据库前，必须先经老板同意；备份目录/文件名必须带版本号（如 `Workbench_backup_V1.0.0`），不得用日期或"latest"代替；若备份的是"待提交中间版本"，须在记录里显眼标注当前版本号与待升版本号的差异。
- **应用内加密备份（方案C，已上线）**：顶栏"📤备份"一键导出整库加密 `.json`（口令加密，别人打不开），存到微信/邮箱/网盘即可。云端真出问题（如项目被删）时，用"📥恢复"选该文件 + 输入同口令即可还原。这是"记牢口令 + 应用内备份"的双重兜底。
- **第四层：私有仓库每日自动备份（已上线）**：独立私有仓库 `fashpig51/hutudan-workbench-backup`，每天**北京时间 03:00** GitHub Actions 自动把云端该 workspace 的表（线上单文件版当前为 **5 张 `wb2_*` 表**；若未来启用多文件版则为 8 张）加密数据拉一份存 `backups/YYYY-MM-DD.json`（密文、口令不进仓库）。本地仓库 `D:\EDG\software\hutudan-workbench-backup`。恢复方式：私有仓库 README 写了"点 Raw 下载 → 工作台恢复"，或紧急情况让打边炉跑 `restore.js` 推回云端。
- 数据库后台备份：Supabase 后台可导出；也可在老板同意下用 `pg_dump`。本地缓存无独立备份必要（云端为主）。
- 本项目与老板另外两个小程序（糊涂蛋多吃饭 / 糊涂蛋的小仓库）**互相独立**，不要混用代码或备份。

---

## 六、未完事项 & 下一步建议（新助手可提方案给老板）

1. （已完成）Supabase 建项目 + 填密钥 + 执行 schema + 部署 GitHub Pages + 配保活 + 三端真机验收。
2. （已完成）专属图标配置（清单版，名字"糊涂蛋工作台"）。
3. （已完成）手机端响应式底栏（方案B）+ 导出/导入加密备份（方案C）+ iPhone 顶栏遮挡修复。
4. （已完成）第四层私有仓库每日自动加密备份（北京时间 03:00，密文、口令不进仓库）。
5. （建议做）Cloudflare 后台项目删除：早期建过但因国内超时弃用，现与现有系统零耦合，删了干净（免费档无费用、无影响）。
6. 全功能已上线（工作/学习/生活/全局均已可用），当前进入细节打磨期：老板集中发小 bug 后逐项修复；后续新增功能（AI 语音付费升级、更精细图表等）均需先出方案等老板点头。
7. UI 细节打磨：图标美术稿精修、各页面手机端适配细节。
8. 若老板未来要"完全离线也要多设备同步"，再评估 P2P/本地网盘方案（当前架构不支持，属重大改动，须单独立项）。
9. **版本号真相 & 当前状态（重要，新助手必读）**：深色重设计 + 日历日/周视图 + 标签全局增删改 这些功能代码确实已写进文件（老板看到的深色版），但**版本号此前漏升**，`config.js` 真源一直停在 `V1.0.32`，git 提交历史也只到 V1.0.32（之前记录里写的"V1.0.36"是误记，未真正对齐/落地版本号）。**本次 V1.0.33 = 在 V1.0.32 基底上修复"新建待办/笔记显示保存了、重进却消失"的 bug**：根因是重进时合并逻辑只认"云端有的 + 标了待补传的"，本地其他项被清掉；已改为"本地与云端都保留、同 id 本地较新优先、已软删不复活"。云端写入失败仍由 `app.js` 的 `onSyncIssue` 弹"没存上云端"提示。 ⚠️ **本条写于架构真相澄清前，需更正**：此处"老板看到的深色版"指多文件设计版（V1.0.36 重设计），但**老板实际日常使用的是线上单文件 `index.html` 版（默认浅色）**，深色那套从未真正部署。版本号真源也已更正为 `index.html` 的 `APP_VERSION`（见开场单第 9 条），`config.js` 是死代码、不是版本真源。
10. **V1.0.33 待老板三端真打开验收**：线上已是 V1.0.33。建议老板在 电脑/苹果/vivo 三端看：①新建待办→锁屏重进→还在；②新建笔记→刷新→还在；③断网新建→重进→还在（本地兜底）；④日历月/周/日切换、标签管理增删改、顶栏不挡、四导航不白屏、小屏不挤。有问题按红线⑩给验收清单逐项核。
11. **V1.0.34 根治实时显示竞争（重要，新助手必读）**：V1.0.33 只修了"重进丢数据"，老板又报三类显示不及时——①保存后列表不立即显示；②进入总览不显示最新、切导航才同步；③笔记首连偶发"保存失败"。根因：`switchTo()` 每次切导航都 `content.innerHTML=''` 整页重建并重新拉云，而全局实时订阅回调 `refreshCurrent()` 也是调 `switchTo(current)`——**自己改一条数据，Supabase 把变更推回来，又触发一次整页重建**，与"刚保存"撞车：重建期拉到的云端数据因复制延迟可能不含刚写的那条，看着像"没保存"。修复：①`switchTo` 改为"每个导航只 build 一次，切换只显隐、不重建"；②`refreshCurrent` 改为只调当前页 `root._render`（刷新数据）、不再重建；③work/study/life/dashboard 四个 section 在 build 时各自暴露 `root._render`（按当前视图刷新对应面板）；④各 section 内部本就已有 `WB.store.subscribe` 订阅自身数据表自动刷新，去掉多余全页重建后实时同步更稳。注意：study/life 是**懒加载**（首次切到才 build），但 build 后即持久不重建。第 10 条验收清单对 V1.0.34 同样适用，且"保存后立刻显示""进入即最新"应已解决。
12. **V1.0.35 根治"刷新后傻等几秒才同步"（重要，新助手必读）**：V1.0.34 修完后，老板又报"刷新出来需要几秒后，才同步"——页面要等云端把所有表拉完才出现。根因：`enterApp()` 里 `await WB.store.pullAll()` 在**画页面之前**执行，等于每次打开先傻等 8 张表云端往返（每张表 `WB.store.list()` 还会在渲染时再拉一遍云，延迟叠加）。修复：①`store.js` 新增 `fastOpen` 开关 + `setFastOpen(v)`，当 `fastOpen=true` 时 `list()` 只取本地缓存、不碰云端；②`enterApp` 改为"先 `buildShell` → 开 `fastOpen` → `switchTo` 用本地缓存秒开页面 → 后台关 `fastOpen` 并异步 `pullAll` + `flushPending` + `refreshCurrent`"；③同步状态灯在后台同步期间显示"云端同步中…"，拉完变"已同步"，让你知道后台还在跑。效果：刷新瞬间本机缓存先出页面（可能先显示上一次的数据），过 1–2 秒云端最新自动刷上来——属于"乐观显示"标准做法，比空白等几秒体验好太多。注意：这只改"打开时怎么读数据"，保存/同步/实时订阅逻辑都没动，无新风险。验收清单：①刷新后页面是否**立刻出现**（不用等）；②出现后约 1–2 秒内是否自动变成最新数据（别的设备刚加的也来了）；③同步灯从"云端同步中"变"已同步"；④保存后立刻显示、进入即最新（V1.0.34 已保证的）仍在。
13. **V1.0.36 根治"重新进入后第一次新建待办闪一下又消失" + 修复"一直浅色"（重要，新助手必读）**：①待办闪消失根因——进主页后台 `pullAll` 拉云端时，`store.list()` 先读一份本机快照，等网络期间老板新建的待办已写进本机缓存，但 `list` 用开头那旧快照 `saveCache` 覆盖，把刚建的误删；第二次建时后台拉云早结束才正常。修复：`list()` 在云端合并后、`saveCache` 之前，**重新读一次本机缓存并把"期间新增的本机项"兜底并回**，无论网络多慢都不会丢老板刚建的。只动 `store.js` 的 `list`，不动 `upsert`/同步逻辑，无新风险。②浅色根因——`app.js` 第166行"若记着浅色偏好就套浅色"，老板以前点过浅色被记死，所以一直浅色（代码默认本就深色）。修复：打开时清掉历史浅色残留偏好，默认回到深色；右上角月亮仍可临时切浅色（重新打开会回深色，若以后要浅色永久再调）。验收清单：①重新进入后第一次新建待办→保存后立刻显示且总数量+1、不再消失；②第一次/第二次新建都正常；③打开默认深色；④之前修的"保存后立刻显示/进入即最新"仍在；⑤三端强刷（清缓存）后验。
14. **V1.0.37 默认改为浅色（重要，新助手必读）**：V1.0.36 把默认改成了深色（清掉浅色残留偏好），但老板验后拍板"默认打开为浅色"。修复：`app.js` 初始化逻辑从"清掉 light 残留、默认深色"改为"默认给 `body` 套 `light`（浅色）；仅当用户记过 `dark` 偏好时才保持深色"。切换按钮本身已会记 `light`/`dark` 偏好（记 `dark` 则下次打开保持深、记 `light` 或没记则浅），无需改。验收清单：①重新打开即浅色；②点右上角月亮切深→记 `dark`→重开还是深；③再点月亮切回浅→重开浅；④之前修的待办闪消失/秒开/实时同步均不受影响。
15. **V1.0.38 根治"总览同步慢/进入拉取慢" + 待办页默认进行中、加"📜 历史"入口（重要，新助手必读）**：老板又报三类——①新增待办后总览页同步慢（要等云端推回才刷新）；②重新进入后待办页拉数据慢、几秒后才出来；③完成的待办混在待办页、要求单独历史入口。根因（存储层）：`list()` 之前在 `fastOpen` 关闭后会去云端等几秒才返回，且存储层**没有本机通知机制**——本机一改只能等云端实时订阅把变更推回来（几秒延迟）。修复（彻底改存储层为"本机优先 + 本地即时通知"）：①`list()` 改为**永远秒回本机缓存**，不再等云端；②新增 `notify(table)` 本地通知 + `_localSubs` 注册表；③`subscribe()` 同时挂"本机订阅"和"跨设备实时订阅"，两者都走"更新本机缓存 → notify 刷新"一条路；④`upsert`/`remove`/`upsertRaw` 写入本机缓存后立即 `notify`，本机改动**秒级**刷新所有页面（含总览）；⑤云端同步全挪后台（`upsert` 异步写云、进主页 `pullAll`→`syncTable` 拉一次、跨设备靠 `realtime`），拉完只更新本机缓存并 `notify`，不再阻塞任何读取；⑥总览页 `WB.dashboard` 新增对 `todos/habits/notes/goals/time_logs` 的本地订阅，任一表变化（含本机新增待办）立刻重渲染总览，不等云端；⑦待办页（`work` 区）默认筛选从 `all` 改为 `active`（做完的不再显示在待办主列表），并把筛选栏"已完成"按钮改名为"📜 历史"（点开看已完成）。风险与验收（底层重构、改动面大，必须三端真机验）：①新增待办→总览"今日待办/已完成"数字**立刻变**、不用等；②重新进入→切到待办页**立刻显示**最新、不卡几秒；③待办页默认只显示进行中、做完的看"📜 历史"；④手机A新建→手机B即时看到（跨设备不能坏）；⑤之前修的"保存后立刻显示/第一次新建不消失/刷新秒开"都还在。
16. **V1.0.39 切到单文件内联版修功能（重要，新助手必读）**：经排查确认线上跑的是 `index.html` 单文件内联版（"Workbench V2"），`assets/js/` 多文件版从未部署、老板从未看到（V1.0.33~V1.0.38 全改在死代码上）。老板选"直接修单文件版"最快最稳。改动（index.html 内联）：①待办筛选栏"已完成"按钮→「📜 历史」按钮（点开看已完成）；②默认高亮从"全部"移到"未完成"；③`state.filter.todo` 从 "all" 改为 "open"（待办页默认只显未完成的）；④`APP_VERSION` 由兜底 "V1.0.28" 改为写死 "V1.0.39"（这套不加载 config.js、原兜底一直生效导致版本号错乱）。主题保持浅色（符合老板"默认浅色"要求）。验收：①待办页默认只显"未完成"；②点「📜 历史」看已完成；③新建待办→总览数字立刻变；④重进切待办页立刻显；⑤浅色默认、右上角可切深；⑥三端强刷验。
17. **V1.0.40 同步引擎升级（重要，新助手必读）**：消除"整批删传"空窗 + 删除传播。根因：旧 `supaPush` 每次"先 DELETE 全表再 POST 全量"，删成功插失败的中间窗口会整批丢数据；且本地删除只从本机 filter 掉、靠整批删传传播，改增量 upsert 后删了的会从云端复活。改动（index.html 内联）：①`supaPush` 改为按编号增量更新（POST + `on_conflict=id` + 合并去重），不再先删全表；②新增 `addPendingDelete(kind,id)` 登记本地删除、`flushPendingDeletes(kind)` 在 syncAll 每表推送后逐条从云端 DELETE、成功才从待删名单移除；③四个删除点（待办/笔记×2/番茄项目）登记 pending；④`state.pendingDeletes` + saveLocal/loadLocal 持久化，刷新不丢。事前用云端实测证明表支持按 id 增量更新+删除。验收：①删除一条待办→其他设备几秒内也消失、不再复活；②断网删→联网后云端也删；③三端验。
18. **V1.0.41 同步加固（最新，新助手必读）**：四刀（改在 index.html 内联版）：①**合并防覆盖**——新增 `mergeById(local,cloud)` 按 id 归并；本地有"未上传标记"(`_dirty`)的记录保本地、不被云端旧版盖掉，无标记的用云端（另一台更新能下来），云端独有/本地独有都保留 → 彻底消除"本地新改动被云端旧版覆盖"和"删了又复活"。②**云端加密**——复用 AES-GCM，待办标题、笔记内容上云前加密（云端只存密文，口令不对读不出；锁定的笔记云端存占位符）。③**轮询 3 秒**——自动同步由每 8 秒改为每 3 秒。④**未记住登录也秒开**——未记住登录时登录框立即出现、云端在后台静默拉取，不挡首屏。⚠️ **关键踩坑**：原想用 `updatedAt` 时间戳做合并，但实测云端 `wb2_todos` 等表**没有 `updatedAt` 列**（推送带它直接 400），故改用本地 `_dirty` 标记（云端零改动）。`_dirty` 全链路：11 处本地改动打标 → 推送前剥掉 `_dirty`/非云端字段 → 推送成功清标 → 合并按标记判定。已用真实云端验证：干净推送 201、带 `_dirty`/updatedAt 推送 400、加密往返正确、合并逻辑单测 6 场景全过、线上 raw 抓到 V1.0.41 生效。验收清单（强刷后三端验）：①本机刚改的不被另一台旧数据覆盖、删了不复活；②待办标题/笔记内容云端是密文；③别的设备一改约 3 秒内同步过来；④未记住登录重开也秒开；⑤之前修的（新建不消失/保存立刻显示/默认浅色/📜历史按钮）仍在。
19. **V1.0.42 手机端备忘录改单视图（重要）**：老板图二反馈手机端编辑/查看区挤成一小条。根因：手机端进笔记后编辑器上下分两半（写、看各占一半）都小。方案A（老板选）：手机端进笔记改成"单视图"——默认整屏看，点"写/看"按钮一键切到整屏写，不再同时挤两样；电脑端三栏（左列表右编辑+预览同显）完全不动。改动（index.html 内联）：①note-editor-head 加"写/看"切换按钮（仅窄屏显示）；②窄屏单栏加 `.note-mode-view`/`.note-mode-edit` 单视图样式（view 隐藏输入框显示预览占满、edit 反之，字号放大行距放宽）；③隐藏旧的全屏按钮（由写/看替代）；④JS 加 `setNoteMode` 切换 + 打开笔记时手机端默认看、新建默认写、电脑端不变；切之前先保存内容刷新预览。已 raw 直读确认 V1.0.42/写看按钮/单视图样式都在 GitHub；GitHub Pages 重建时偶发卡顿，强刷后验。验收（手机端强刷后）：①进笔记整屏显示、不再上下两半挤；②默认看模式字号大行距宽、长文可滚；③点"写"整屏输入框、软键盘不挡；④点"看"立刻看到刚写内容；⑤电脑端三栏布局不变；⑥三端（电脑/苹果/vivo）新建/编辑/查看/删除笔记正常。

20. **V1.0.43 笔记查看页列表项改数字序号（最新）**：老板反馈笔记查看页里他手标的星星（用来区分）和列表项默认的小圆点显示不一致。问题根因：查看页用极简 Markdown 渲染，以短横线 `- ` 开头的行被当成列表、浏览器默认显示圆点，而他直接写的 `★` 字符保持原样，同一篇里两种标记混着显。改动（index.html 内联）：①`.md-ul/.md-ol` 加 `list-style: decimal`、新增 `.md-li` 间距，让列表项显示 1、2、3 数字序号而不是小圆点；②微调 `renderMarkdown` 把有序列表（数字. ）的 `li` 也正确包进列表容器（原代码因执行顺序导致零散 `li` 没被包裹、会显示成异常圆点）。**不动老板手标的 `★`**（那是他自己的标注）。已 raw 直读确认线上含 V1.0.43 与 `list-style: decimal`。验收（手机端强刷后）：①笔记里 `- ` 开头的列表项现在显示 1、2、3 序号；②老板手标的 `★` 仍按原样显示（不被系统改）；③纯数字有序列表（如 `1. xxx`）也正常编号、不再零散；④电脑端三栏不变、三端查看笔记正常。

21. **V1.0.44 笔记查看页列表保留原符号缩进（最新）**：老板反馈 V1.0.43 的数字序号有两个问题——①`- ` 列表项被手标的 `★` 行隔断、每段重新从1数，看着"全是1"；②加了 1、2、3 反而看不出哪些内容是一组、不好区分上下文。改动（index.html 内联）：①`renderMarkdown` 不再把 `- `/`数字. ` 转成浏览器原生 `<ul><li>`（那套会自动编号/加圆点），改成包成自定义 `<div class="md-list">`/`<div class="md-item">`，**原样保留你写的符号（★/-/数字.）**，不再自动编 1、2、3；②CSS 去掉 `list-style: decimal`、改 `.md-list`(缩进20px)+`.md-item`(行距放宽)；③段落包裹逻辑把 `div` 也加入"跳过"名单，避免列表被多余段落标签包住。老板手标的 `★` 仍原样显示、不被系统改。已 raw 直读确认线上含 V1.0.44 与 `md-item`/`md-list` 样式。验收（手机端强刷后）：①笔记里 `- ` 项现在显示你写的 `- ` 符号、缩进分组，不再是 1、2、3；②手标 `★` 仍原样显示；③`1. xxx` 这类你手写的有序项也按你写的显示、不再被系统重排；④上下文分组清晰、不被序号打断；⑤电脑端三栏不变、三端查看笔记正常。

22. **V1.0.45 有序数字项前面加圆点（最新）**：老板再看截图后反馈，想在自己写的有序数字项（如 `1. 大学生职业生涯规划...`）前面再补一个圆点标记，`★` 项和 `- ` 项保持原样。改动（index.html 内联）：①`renderMarkdown` 列表逻辑拆成两条——先处理 `数字. ` 项，输出 `• 1. xxx`（前面加圆点，保留你写的数字），再处理 `★`/` -` 项原样输出；②`md-list`/`md-item` 缩进分组逻辑不变。已 raw 直读确认线上含 V1.0.45 与 `• `（圆点前缀）。验收（手机端强刷后）：①`1. xxx`/`2. xxx` 这类有序项前面显示小圆点 `•`；②`★` 项和 `- ` 项仍原样显示；③分组缩进清晰；④电脑端三栏不变、三端查看笔记正常。

23. **V1.0.46 圆点项子说明文字缩进两格（最新）**：老板反馈 V1.0.45 的圆点项（如 `• 1. 大学生职业生涯规划...`）下面紧接的普通说明文字（如"找不到..."）和圆点项平齐、不好区分从属关系，希望子说明往右空两格。改动（index.html 内联）：①`renderMarkdown` 在列表处理之后、段落包裹之前新增一道"子内容缩进"逻辑——逐行扫描，凡是紧跟在 `md-item`（圆点项/列表项）下面、且中间没有空行的普通行，自动包成 `<div class="md-sub">`（左边距约两格、颜色略淡），表示是圆点项的子说明；遇到空行或新的结构标签（标题/引用/列表项）则结束缩进；②CSS 新增 `.md-sub { margin-left:26px; color:var(--ink-soft) }`。`★` 项和 `- ` 项本身不变，圆点项本身不变。已 raw 直读确认线上含 V1.0.46 与 `md-sub` 样式。验收（手机端强刷后）：①`• 1. 大学生...` 项下面紧接的"找不到..."等说明文字向右空两格显示；②若说明文字与圆点项之间隔了空行则不缩进（属正常，如需缩进请告知）；③`★`/`- ` 项与有序项均不受影响；④电脑端三栏不变、三端查看笔记正常。

24. **V1.0.47 圆点放大（最新）**：老板再看截图反馈，圆点项（如 `• 1. 大学生职业生涯规划...`）前面的小圆点 `•` 显示太小，希望做大一点；同时确认子说明缩进效果（第二行"找不到..."比第一行空）是对的。改动（index.html 内联）：①`renderMarkdown` 处理有序数字项时，把前面的圆点 `•` 包进 `<span class="md-dot">`；②CSS 新增 `.md-dot` 样式，用放大到 1.3 倍（约大一圈），并稍微往右调间距，让圆点更明显；③保留 `V1.0.46` 的子说明缩进逻辑和 `md-sub` 样式不变。`★` 项和 `- ` 项仍原样，圆点项本身除圆点大小外不变。已 raw 直读确认线上含 V1.0.47 与 `md-dot` 样式。验收（手机端强刷后）：①有序数字项 `1. xxx`/`2. xxx` 前面的小圆点明显变大；②子说明文字仍向右空两格；③`★`/`- ` 项不受影响；④电脑端三栏不变、三端查看笔记正常。

25. **V1.0.50 子说明固定间距（定稿）**：V1.0.48 未经老板点头即推送、把子说明 `md-sub` 缩进从 26px 误调到 15px，导致第二行 `❌ 找不到...` 比第一行"1."偏左；V1.0.49 改为 44px 后老板验收发现仍和"1."同一列（圆点放大后占位变宽，44px 正好与"1."对齐）。改动（index.html 内联）：①`.md-sub` 的 `margin-left` 由 44px 改为 60px，确保第二行明显比第一行"1."右空一格；②`APP_VERSION` 升 V1.0.49→V1.0.50；③`assets/js/config.js`（死代码那套）同轮对齐到 V1.0.50。已 raw 直读确认线上含 V1.0.50 与 `margin: 2px 0 5px 60px`。验收（手机端强刷后）：①第二行 `❌ 找不到...` 明显比第一行"1."再右空一格；②`★`/`- ` 项与圆点项不受影响；③电脑端三栏不变、三端查看笔记正常。

26. **V1.0.51 子说明缩进加到 120px（含一次推送事故教训）**：老板验收 V1.0.50（60px）仍说"没有任何区别、像没改过"。先查实：①红线自查踩了第 16 条——同一缩进问题已连改多轮（26→44→60），未先停查根因；②老板选 B 方案，确认把 `md-sub` 的 `margin-left` 从 60px 加到 120px。改动（index.html 内联）：`.md-sub` 改为 `margin: 2px 0 5px 120px`；`APP_VERSION` 升 V1.0.50→V1.0.51；`assets/js/config.js`（死代码那套）同轮对齐到 V1.0.51。**⚠️ 重大事故：本次首次推送脚本漏传 GitHub 接口必需的 `sha` 文件标识，两个文件 PUT 均返回 422 失败（"sha wasn't supplied"），导致线上一直是旧 V1.0.50/60px——这正是老板"没变化"的根因之一（非纯缓存）。修正脚本（先 GET 拿 sha 再 PUT）后重推成功，raw 直读确认线上含 V1.0.51 与 `margin: 2px 0 5px 120px`。** 教训：以后任何 GitHub API 文件推送脚本，必须带 `sha`；推完要 raw 直读线上确认真的变了，不能只看"脚本跑完"。验收（手机端强刷后）：①第二行 `❌ 找不到...` 明显比第一行正文右空一格（120px 足够明显）；②圆点项 `•` 仍放大；③`★`/`- ` 项不受影响；④电脑端三栏不变、三端查看笔记正常。

27. **V1.0.52 笔记查看页子说明真正生效（根因修复，最重要的一条）**：老板验收 V1.0.51（120px）仍说"还是没变化"。这次查实**真根因不在缩进数值、也不在缓存**：`renderMarkdown` 里「把连续列表项包进 `md-list`」这一步（原第2122行）排在「子说明检测」之前，md-list 包裹正则把 md-item 行变成了 `<div class="md-list"><div class="md-item">...`，开头不再是 `md-item`，导致后续子说明检测认不出"上一行是 item"，`_inItem` 始终 false，`❌`/`✔️` 那两行**从未被包成 `md-sub`**——所以无论把 margin 调成 60 还是 120 都无效（元素根本没有这个样式）。**修复**：把 md-list 包裹移到子说明检测之后，让子说明先识别到独立的 md-item 行，再包 md-list。用真实 index.html 抽出函数跑老板笔记结构验证：`❌找不到...` 现已正确输出为 `<div class="md-sub">❌找不到，采购 ¥32.3</div>`，md-dot 圆点也在。改动（index.html 内联）：调整 renderMarkdown 内列表/子说明/md-list 三段顺序；`APP_VERSION` 升 V1.0.51→V1.0.52；`assets/js/config.js`（死代码那套）同轮对齐到 V1.0.52。部署脚本带 sha 推送，raw 直读确认线上含 V1.0.52 与 `md-sub { margin: 2px 0 5px 120px }`。**结论：之前 V1.0.46~V1.0.51 五轮调数值全是白调，真正要修的是渲染顺序 bug；老板说"没变化"每次都是真的——因为那两行一直没被加样式。** 验收（手机端强刷后）：①第二行 `❌ 找不到...` 现在明显比第一行右空一格（这次是真生效）；②第四行 `✔️ 拼多多...` 也比第三行右空一格；③圆点 `•` 仍放大；④`★`/`- ` 项不受影响；⑤电脑端三栏不变、三端查看笔记正常。

28. **V1.0.53 子说明缩进调回 76px（按划线位置固定间距）**：老板验收 V1.0.52（120px）说"有变化了，但是有点夸张了"，并截图划线——希望第二行 `❌ 找不到...` 停在第一行"大学生"的"大"字那一列，不要缩进太多。改动（index.html 内联）：`.md-sub` 的 `margin-left` 从 120px 调回 **76px**（固定间距，不再动态对齐）；`APP_VERSION` 升 V1.0.52→V1.0.53；`assets/js/config.js`（死代码那套）同轮对齐到 V1.0.53。部署脚本先 GET 拿 sha 再 PUT，raw 直读确认线上含 V1.0.53 与 `md-sub { margin: 2px 0 5px 76px }`。验收（手机端强刷后）：①第二行 `❌ 找不到...` 不再像 120px 那样夸张，大致对齐到第一行"大"字位置；②圆点 `•` 仍放大；③`★`/`- ` 项不受影响；④电脑端三栏不变、三端查看笔记正常。

29. **V1.0.54 有序项正则兼容无空格写法（真·最后一次根因）**：老板验收 V1.0.53(76px)再次说"又没变化了"。这次没闷头调数字，先抽真实 `renderMarkdown` 用老板笔记两种写法跑测试——**关键发现**：若老板写的是 `1.大学生`（数字点后面**无空格**，从其截图 `1.大学生职业生涯规划` 看高度吻合），原正则 `^(\d+\.) ` 要求点后必须有空格，匹配不上 → 整个笔记塌成一段 `<p>`，`❌`/`✔️` 子说明行**从未被包成 md-sub**，所以无论 margin 设 26/44/60/120/76 全无效（连 V1.0.52 修复的渲染顺序也救不了，因为根本没生成 item 行）。**修复**：有序项正则改为 `^(\d+\.)\s*(.*)$`（`\s*` 兼容有无空格），用真实函数验证：无空格写法现也能正确输出 `<div class="md-sub">❌找不到...</div>`；带空格写法不受影响。改动（index.html 内联）：renderMarkdown 有序项正则 `\d+\.) ` → `\d+\.)\s*`；`APP_VERSION` 升 V1.0.53→V1.0.54；`assets/js/config.js`（死代码那套）同轮对齐到 V1.0.54。部署脚本带 sha 推送，raw 直读确认线上含 V1.0.54 与新正则。⚠️ **结论**：V1.0.46~V1.0.53 共七轮调缩进/修复，若老板用的是无空格写法，实际全白调——根因一直是无空格导致整篇塌段。验收（手机端**务必彻底清缓存/清网站数据**后）：①`1.大学生`/`1. 大学生` 两种写法下，`❌ 找不到...` 都明显比第一行右缩进（76px）；②圆点`•`仍放大；③`★`/`- ` 项不受影响；④电脑端三栏不变、三端查看笔记正常。

---

## 附录：16 条红线（原 10 条 + 新增 11–16 条，同等最高纪律，触碰=失职）

① 先出主意再动手：先把要做的、为啥、有啥风险说清楚，老板说"行"才改文件。— 和第 ⑪、⑦、⑭、⑬ 条同源，归为〔质量闭环〕（开头）：先商量后动工。
② 只做老板点名的事：老板没说的别自作主张去碰。另：老板问的先解决老板问的那件事，再去做别的，别顺手加戏——先把老板的问题本身办妥，不擅自延伸。— 和第 ⑨、⑮ 条同源，归为〔权限边界〕：没拿到明确同意或不知道改哪之前不动手。
③ 说话全用大白话：绝对不许说专业术语，也不许蹦英文词（代码里的变量名、命令、缩写等一律不主动提）；碰到不好懂的词，必须马上用大白话补一句说明。— 和"说人话规矩"、第 ④ 条同源，归为〔沟通纪律〕：跟老板说的话要一听就懂。
④ 老板说得不对可以顶，但先自己查清楚再顶。— 和第 ③ 条、"说人话规矩"同源，归为〔沟通纪律〕：懂归懂，不对的就拿事实说话。
⑤ 大方向说得含糊就先问明白，别自己瞎定；老板问起来，先给办法，老板准了再动手。— 和第 ⑥、⑩、⑫ 条同源，归为〔协作姿态〕：问清再干，少让老板返工。
⑥ 活全你干（写、试、发上网、自己验证），别推给老板。— 和第 ⑤、⑩、⑫ 条同源，归为〔协作姿态〕：能自己扛就自己扛。
⑦ 先查实情再回答（读现有文件、把云端数据拉出来看），别凭印象猜。— 和第 ①、⑪、⑭、⑬ 条同源，归为〔质量闭环〕（过程中验证）：改完要真打开跑一遍。
⑧ 眼光放长远，每次改动先想会牵动啥。— 归为〔全局视角〕独立条：每次改动往前多想一步，和上面四组都不冲突。
⑨ 改界面样子必须先经老板同意。— 和第 ②、⑮ 条同源，归为〔权限边界〕：改界面是"只做点名的事"在画面上的特例。
⑩ 每次改完，把要检查的点（在手机/电脑真打开看的清单）发给老板。— 和第 ⑤、⑥、⑫ 条同源，归为〔协作姿态〕（收尾交代）：附上验收清单。
⑪ 动手前先确认文件里现在到底是什么样：改之前必须读一遍当前文件 / 翻改动记录本，确认要改的地方真在那里、没被早先的改动挪走过，不准凭"上次改过那行"直接下手。— 和第 ①、⑦、⑭、⑬ 条同源，归为〔质量闭环〕（动手前核实）：改之前先看清现状。
⑫ 主文件和备份文件要同一轮一起改完：凡项目有主文件又有一份备份文件（比如 `index.html` 和 `index_v2.html`），任何改动必须两个文件在同一轮、一起改好、一起发上网，不许只改一处就上线、不许过好几轮才补同步。— 和第 ⑤、⑥、⑩ 条同源，归为〔协作姿态〕：备份文件同轮同步，别让老板发现漏了。
⑬ 动了网页样子或图片的交代必须提醒清缓存重看：凡改动涉及网页样式、图片这类会被手机/电脑记住旧版本的内容，验收清单里必须单独写"请清掉缓存（下拉刷新 / 清历史）再验收"，不能想当然以为老板会自动想到。— 和第 ①、⑪、⑦、⑭ 条同源，归为〔质量闭环〕（收尾提醒）：改样子必提醒清缓存。
⑭ 发上网后必须抓网上实际内容验证才算交差：发完必须去网上把实际打开的内容抓下来，确认版本号和这次改动真的生效了（比如网页托管平台已重新生成），不能只在本机跑通就说交了；网上没确认之前不准说"已上线"。— 和第 ①、⑪、⑦、⑬ 条同源，归为〔质量闭环〕（收尾验证）：网上抓到证据才算交付。
⑮ 指代不清必须当场问清：老板话里提到某个东西但没说清是哪个时（比如"按总览页前面那样""和之前一样"），必须先反问"你说的是哪个页面的哪一块"，确认具体目标再动手，不自己瞎猜指代。— 和第 ②、⑨ 条同源，归为〔权限边界〕：指代不清就是对象不明，先确认再动手。
⑯ 连改两轮还没解决就先查原因，严禁闷头连改超过两轮：同一个问题连改两轮还没好，严禁闷头瞎改第三轮；先回头看前两次改了啥、为啥没效，从这两回的改动一步步排查病根，优先自己排查；自己真排不出来的，再去问老板，不提前问、也不无限试错。— 和第 ⑧ 条同源，归为〔实据行事〕：两轮没解决先查前两次病根，严禁闷头连改超两轮。

> 注：本文件本身不存任何密钥、口令、密码。Supabase 密钥由老板填在 `config.js`；口令只存在于老板脑中。新助手切勿把密钥写进文档或提交到公开仓库。
> GitHub 仓库 `fashpig51/hutudan-workbench` 是**公开**的，但公开无妨：仓库里的 Supabase 钥匙本就是设计给前端用的（真正保护靠数据库访问限制 + 口令加密）。

